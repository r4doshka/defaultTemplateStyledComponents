import styled, { css } from 'styled-components';

export const HeaderStyled = styled.header`
    height: 60px;
    display: flex;
    align-items: center;
    background: #ffffff;
    position: fixed;
    right: 0;
    left: 0;
    z-index: 99;
`;
